import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.huawei.digitalclass',
  appName: 'Huawei Digital Class',
  webDir: 'out'
};

export default config;
